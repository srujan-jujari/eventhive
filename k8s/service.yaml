apiVersion: v1
kind: Service
metadata:
  name: eventhive-service
spec:
  type: NodePort
  selector:
    app: eventhive
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30000
